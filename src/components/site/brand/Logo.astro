<svg class="w-14 h-14 border border-background">
  <defs>
    <radialGradient id="logo-gradient" cx="50%" cy="50%" r="40%">
      <stop offset="0%" stop-color="var(--black)"></stop>
      <stop offset="100%" stop-color="var(--foreground)"></stop>
    </radialGradient>
  </defs>
  <rect width="100%" height="100%" fill="url(#logo-gradient)"></rect>
</svg>
